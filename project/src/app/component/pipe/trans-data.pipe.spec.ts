import { TransDataPipe } from './trans-data.pipe';

describe('TransDataPipe', () => {
  it('create an instance', () => {
    const pipe = new TransDataPipe();
    expect(pipe).toBeTruthy();
  });
});
